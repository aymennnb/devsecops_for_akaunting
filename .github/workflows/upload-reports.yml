#upload-to-defectdojo:
#  name: Upload Security Scans to DefectDojo
#  runs-on: ubuntu-latest
#  needs: [semgrep-sast, dependency-check, gitleaks]
#
#  steps:
#    - name: Checkout code
#      uses: actions/checkout@v4
#
#    - name: Download Semgrep artifacts
#      uses: actions/download-artifact@v4
#      with:
#        name: sast-reports-${{ github.sha }}
#        path: ./sast-reports
#      continue-on-error: true
#
#    - name: Download Dependency Check artifacts
#      uses: actions/download-artifact@v4
#      with:
#        name: dependency-check-reports-${{ github.sha }}
#        path: ./dependency-check-reports
#      continue-on-error: true
#
#    - name: Download GitLeaks artifacts
#      uses: actions/download-artifact@v4
#      with:
#        name: gitleaks-reports-${{ github.sha }}
#        path: ./gitleaks-reports
#      continue-on-error: true
#
#    - name: Upload Semgrep results to DefectDojo
#
#      env:
#        DEFECTDOJO_URL: ${{ vars.DEFECTDOJO_URL }}
#        DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
#        DEFECTDOJO_ENGAGEMENT_ID: ${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}
#      run: |
#        echo "== Uploading Semgrep results to DefectDojo =="
#        echo "DefectDojo URL: ${{ vars.DEFECTDOJO_URL }}"
#
#        SEMGREP_REPORT="sast-reports/semgrep-general.json"
#
#        if [ -f "$SEMGREP_REPORT" ]; then
#          echo "Found Semgrep report: $SEMGREP_REPORT"
#
#          curl -X POST \
#            "${{ vars.DEFECTDOJO_URL }}/api/v2/import-scan/" \
#            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
#            -H "Content-Type: multipart/form-data" \
#            -F "engagement=${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}" \
#            -F "scan_type=Semgrep JSON Report" \
#            -F "file=@$SEMGREP_REPORT" \
#            -F "verified=true" \
#            -F "active=true"
#
#          echo "== Semgrep results uploaded successfully =="
#        else
#          echo "Semgrep report not found at $SEMGREP_REPORT"
#        fi
#
#    - name: Upload Dependency Check results to DefectDojo
#
#      env:
#        DEFECTDOJO_URL: ${{ vars.DEFECTDOJO_URL }}
#        DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
#        DEFECTDOJO_ENGAGEMENT_ID: ${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}
#      run: |
#        echo "== Uploading Dependency Check results to DefectDojo =="
#
#        DEPENDENCY_REPORT="dependency-check-reports/dependency-check-report.json"
#
#        if [ -f "$DEPENDENCY_REPORT" ]; then
#          echo "Found Dependency Check report: $DEPENDENCY_REPORT"
#
#          curl -X POST \
#            "${{ vars.DEFECTDOJO_URL }}/api/v2/import-scan/" \
#            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
#            -H "Content-Type: multipart/form-data" \
#            -F "engagement=${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}" \
#            -F "scan_type=Dependency Check JSON Report" \
#            -F "file=@$DEPENDENCY_REPORT" \
#            -F "verified=true" \
#            -F "active=true"
#
#          echo "== Dependency Check results uploaded successfully =="
#        else
#          echo "Dependency Check report not found at $DEPENDENCY_REPORT"
#        fi
#
#    - name: Upload GitLeaks results to DefectDojo
#
#      env:
#        DEFECTDOJO_URL: ${{ vars.DEFECTDOJO_URL }}
#        DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
#        DEFECTDOJO_ENGAGEMENT_ID: ${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}
#      run: |
#        echo "== Uploading GitLeaks results to DefectDojo =="
#
#        GITLEAKS_REPORT="gitleaks-reports/leaks.json"
#
#        if [ -f "$GITLEAKS_REPORT" ]; then
#          echo "Found GitLeaks report: $GITLEAKS_REPORT"
#
#          curl -X POST \
#            "${{ vars.DEFECTDOJO_URL }}/api/v2/import-scan/" \
#            -H "Authorization: Token ${{ secrets.DEFECTDOJO_API_KEY }}" \
#            -H "Content-Type: multipart/form-data" \
#            -F "engagement=${{ vars.DEFECTDOJO_ENGAGEMENT_ID }}" \
#            -F "scan_type=Generic JSON Report" \
#            -F "file=@$GITLEAKS_REPORT" \
#            -F "verified=true" \
#            -F "active=true"
#
#          echo "== GitLeaks results uploaded successfully =="
#        else
#          echo "GitLeaks report not found at $GITLEAKS_REPORT"
#        fi
